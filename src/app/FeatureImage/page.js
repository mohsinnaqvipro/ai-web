import React from 'react'
import FeatureImage from '@/components/FeatureImage'

const FeatureImagePage = ({ flag, content }) => {
  return <FeatureImage flag={flag} content={content} />
}

export default FeatureImagePage
